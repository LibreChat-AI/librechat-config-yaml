version: 1.2.1

cache: true

interface:
  customWelcome:
    "Notice: This `librechat.yaml` file is deprecated. Update your `CONFIG_PATH` environment variable to: 
    `https://raw.githubusercontent.com/LibreChat-AI/librechat-config-yaml/main/librechat-env.l.yaml`"

registration:
  socialLogins:
    - "discord"
    - "facebook"
    - "github"
    - "google"
    - "openid"