version: 1.2.1

cache: true
fileStrategy: "firebase"

registration:
  socialLogins:
    - "discord"
    - "facebook"
    - "github"
    - "google"
    - "openid"

interface:
  customWelcome:
    "Notice: This `librechat.yaml` file is deprecated. Update your `CONFIG_PATH` environment variable to: 
    `https://raw.githubusercontent.com/LibreChat-AI/librechat-config-yaml/main/librechat-env-f.yaml`"